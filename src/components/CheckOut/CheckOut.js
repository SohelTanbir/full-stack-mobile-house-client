import React from 'react';
import { useParams } from 'react-router';

const CheckOut = (props) => {
    console.log(props)
    return (
        <div>
            <h3>CheckOut page ID:</h3>
        </div>
    );
};

export default CheckOut;